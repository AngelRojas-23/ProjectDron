{
  "name": "streaming-dron",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,streaming,frontend \"pnpm --filter backend dev\" \"pnpm --filter streaming dev\" \"pnpm --filter frontend dev\"",
    "start": "concurrently -n backend,streaming,frontend \"pnpm --filter backend start\" \"pnpm --filter streaming dev\" \"pnpm --filter frontend preview\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}